version: "3"

tasks:
  dump-source:
    cmds:
      - uv run dumpster-cli

  test:
    cmds:
      - uv run pytest

  release:
    cmds:
      - uv run semantic-release -v version --no-vcs-release
      - git push
      - git push --tags
